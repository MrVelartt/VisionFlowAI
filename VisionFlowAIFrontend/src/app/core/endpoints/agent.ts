import { API_URL } from "@env/environment";

export class AgentEndpoint {
  static readonly agent1 = `${API_URL}agent1`;
  static readonly agent2 = `${API_URL}agent2`;
  static readonly agent3 = `${API_URL}agent3`;
}